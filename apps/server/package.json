{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rm -rf dist && npm run build && nodemon ./dist/src/index.js",
    "build": "tsc",
    "test": "jest --coverage",
    "lint": "eslint src --fix",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nodesoft/shared-config": "^0.0.1",
    "@types/express": "4.17.13",
    "nodemon": "2.0.16"
  },
  "dependencies": {
    "express": "4.18.1"
  }
}
